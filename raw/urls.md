# URLs to Ingest

Add URLs below, one per line. Then tell OpenCode: `/ingest` and it will process them.

You can also pass a URL directly: `/ingest https://example.com/article`

## Queue

<!-- Add URLs below this line, one per line -->
